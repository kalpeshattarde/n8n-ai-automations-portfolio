# Production Improvements

- Replace CSV files with scheduled exports from CRM, billing, or order management systems.
- Load cleaned records into PostgreSQL, Supabase, BigQuery, or a warehouse table.
- Add data contracts for required fields and allowed statuses.
- Add alerting when dirty rows exceed threshold or revenue drops week over week.
- Publish dashboard to an internal portal or BI tool.
- Add owner-level collections tasks for overdue invoices.

