# Support PostgreSQL Setup

The support workflow stores tickets, approved KB articles, drafted responses, approval tasks, and workflow errors.

Local connection:

```text
postgresql://akcelerate_app:akcelerate_support_password@localhost:5434/akcelerate_support
```

Run:

```powershell
cd project_02_ai_support_rag_agent/database
docker compose up -d
```

